# token.py
# JWT tokens are NOT used in this project.
# Authentication is handled entirely by Firebase on the frontend.
# The backend identifies users via their firebase_uid passed in request bodies.
# If you switch to custom JWT auth in the future, implement it here using python-jose.